VocabAru9 Safari compatibility fix.
Upload all files to the ROOT of the GitHub VocabAru repository and overwrite existing files.
supabase-config.js is intentionally excluded so your working Supabase URL/key remains untouched.
Main fix: older Safari without structuredClone no longer crashes before Dashboard rendering.
